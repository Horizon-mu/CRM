<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE configuration
        PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-config.dtd">
<configuration>
    <!--settings 控制mybatis全局行为-->
    <settings>
        <!--设置mybatis输出日志-->
        <setting name="logImpl" value="STDOUT_LOGGING"/>
    </settings>
    <!--设置别名-->
    <typeAliases>
        <!--name: 实体类所在包名-->
        <package name="com.hrz.crm.settings.entity"/>
        <package name="com.hrz.crm.workbench.entity"/>
    </typeAliases>
    <!--sql mapper (sql映射文件)的位置-->
    <mappers>
        <!--name 是包名，这个包中的所有mapper.xml文件一次都能加载-->
        <package name="com.hrz.crm.settings.dao"/>
        <package name="com.hrz.crm.workbench.dao"/>
    </mappers>
</configuration>
        <!--
            mybatis的主配置文件 ： 主要定义了数据库的配置信息。SQL映射文件的位置
            1.约束文件
                <!DOCTYPE configuration
                    PUBLIC "-//mybatis.org//DTD Config 3.0//EN"
                    "http://mybatis.org/dtd/mybatis-3-config.dtd">
            mybatis-3-config.dtd 约束文件名称

            2.configuration 根标签
        -->