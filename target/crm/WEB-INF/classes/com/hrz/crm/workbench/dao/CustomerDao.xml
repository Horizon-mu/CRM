<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.hrz.crm.workbench.dao.CustomerDao">
    <select id="getCustomerByName" resultType="Customer">
        SELECT * from tbl_customer
    </select>

    <insert id="save" parameterType="Customer">
        insert into tbl_customer(
            id,
            owner,
            name,
            website,
            phone,
            createBy,
            createTime,
            editBy,
            editTime,
            contactSummary,
            nextContactTime,
            description,
            address
        )
        values(
            #{id},
            #{owner},
            #{name},
            #{website},
            #{phone},
            #{createBy},
            #{createTime},
            #{editBy},
            #{editTime},
            #{contactSummary},
            #{nextContactTime},
            #{description},
            #{address}
              )
    </insert>
</mapper>